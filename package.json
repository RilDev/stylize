{
  "name": "@rildev/minimal-css",
  "version": "1.0.6",
  "description": "Make your prototypes look good with a standard minimal css stylesheet (< 1Kb)!",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx http-server ./ -o",
    "build": "uglifycss style.css > style.min.css",
    "postbuild": "git add style.min.css",
    "prepublish": "npm run build && git add package.json && git commit --allow-empty -m \"prepare to publish package\"",
    "publish": "git switch -C package && rm -rv * && git checkout origin/master style.min.css && git checkout origin/master package.json && mv style.min.css style.css && git add -A && git commit --allow-empty -m \"package ready for publishing\" && git push -f origin package && npm publish && git switch master",
    "postpublish": "npm i"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RilDev/minimal-css.git"
  },
  "keywords": ["css", "minimal", "prototype"],
  "author": "RilDev",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/RilDev/minimal-css/issues"
  },
  "homepage": "https://github.com/RilDev/minimal-css#readme",
  "devDependencies": {
    "uglifycss": "0.0.29"
  },
  "dependencies": {}
}
