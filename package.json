{
  "name": "@rildev/minimal-css",
  "version": "1.0.7",
  "description": "Make your prototypes look good with a standard minimal css stylesheet (< 1Kb)!",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx http-server ./ -o",
    "build": "uglifycss style.css > style.min.css",
    "postbuild": "git add style.min.css",
    "prepare": "npm run build && npm version patch && git add package.json && git commit --allow-empty -m \"prepare to publish package\"",
    "publish": "git switch -C package && rm -rv index.html style.css assets && mv style.min.css style.css && git add -A && git commit --allow-empty -m \"package ready for publishing\" && rm style.min.css && git push -f origin package && npm publish && git switch master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RilDev/minimal-css.git"
  },
  "keywords": [
    "css",
    "minimal",
    "prototype"
  ],
  "author": "RilDev",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/RilDev/minimal-css/issues"
  },
  "homepage": "https://github.com/RilDev/minimal-css#readme",
  "devDependencies": {
    "uglifycss": "0.0.29"
  },
  "dependencies": {}
}
