{
  "name": "@rildev/minimal-css",
  "version": "2.4.0",
  "description": "Make your prototypes look good with a standard minimal css stylesheet (< 1Kb)!",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx http-server ./ -o",
    "package": "npm version --no-git-tag-version ${VERSION-patch} && git add package.json && git commit -m \"update version number\" && git switch -C package && uglifycss style.css > style.min.css && rm style.css && mv style.min.css style.css && rm -rf index.html assets && git add -A && git commit -m \"ready to publish\" && git push -f origin package && npm publish && git switch master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RilDev/minimal-css.git"
  },
  "keywords": [
    "css",
    "minimal",
    "prototype"
  ],
  "author": "RilDev",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/RilDev/minimal-css/issues"
  },
  "homepage": "https://github.com/RilDev/minimal-css#readme",
  "devDependencies": {
    "uglifycss": "0.0.29"
  },
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "if git-branch-is master; then uglifycss style.css > style.min.css && git add style.min.css; fi"
    }
  }
}
